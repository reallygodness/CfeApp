<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:layout_marginTop="20dp"
    android:padding="10dp">

    <!-- Верхняя панель: адрес пользователя и кнопка изменения -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="8dp">
        <TextView
            android:id="@+id/addressTextView"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="Москва, ул. Ленина 10"
            android:textSize="16sp"
            android:textStyle="bold" />
        <ImageView
            android:id="@+id/editAddressButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_edit" /> <!-- Здесь ваша иконка редактирования -->
    </LinearLayout>

    <!-- Поисковая строка -->
    <EditText
        android:id="@+id/searchEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Найти товар..."
        android:padding="12dp"
        android:background="@drawable/search_edittext_bg"
        android:drawableStart="@drawable/ic_search" />

    <!-- Табы категорий -->
    <LinearLayout
        android:id="@+id/categoryTabs"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:paddingTop="8dp"
        android:paddingBottom="8dp">
        <Button
            android:id="@+id/drinkTabButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Кофе" />
        <Button
            android:id="@+id/dishTabButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:text="Блюда" />
        <Button
            android:id="@+id/dessertTabButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:text="Десерты" />
    </LinearLayout>

    <!-- RecyclerView для каталога -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/catalogRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

</LinearLayout>
